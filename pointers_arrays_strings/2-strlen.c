#include "main.h"
#include <string.h>
/**
  * _strlen - Function that returns the length of a string.
  * @s: string to check
  * Return: length of string
  */
int _strlen(char *s)
{
	int i = strlen(s);

	return (i);
}
