#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
  * _abs- prints the absolute value of an interger
  * @n:the number to check
  *
  * Return: 0 (Success)
  */
int _abs(int n)
{
	int i = abs(n);

	return (i);
}
